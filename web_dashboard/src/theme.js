import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: { mode: "light", primary: { main: "#0B6E4F" }, secondary: { main: "#0B79C9" } },
  shape: { borderRadius: 14 }
});
